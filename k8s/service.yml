apiVersion: v1
kind: Service
metadata:
  name: sentiment-api
  namespace: ml-serving
  labels:
    app: sentiment-api
spec:
  selector:
    app: sentiment-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
